/** @format */

export interface LocationConfig {
  // id: string;
  lat: number;
  lng: number;
  name: string;
  description?: string;
  opacity?: number;
  size?: number;
  url?: string;
  target?: string;
}
